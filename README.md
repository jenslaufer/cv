# cv.jenslaufer.com

Online-CV von Jens Laufer (freelance Fullstack-Entwickler), recruiter-facing.

- `index.html` — self-contained CV-Seite (inline CSS, druckoptimiert → PDF via Browser-Druck).
- `cv.pdf` — vorgerendertes PDF (aus dem Print-Stylesheet, A4).
- `CNAME` — GitHub-Pages-Custom-Domain.

Verlinkt aus dem launch-kit `freelance`-Tenant (`cv-versand`-Autoresponder an den `recruiters`-Verteiler).

Quelldaten: freelancermap-Profilexport (nicht eingecheckt, `data.md`).
