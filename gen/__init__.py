"""CV generator — single source of truth is data/*.csv.

The base CV and every tailored variant are rendered from data/*.csv.
Nothing downstream is hand-edited, so all outputs stay in sync with the source.
"""
