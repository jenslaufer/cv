"""CV generator — single source of truth is data.md.

The base CV and every tailored variant are rendered from data.md.
Nothing downstream is hand-edited, so all outputs stay in sync with the source.
"""
